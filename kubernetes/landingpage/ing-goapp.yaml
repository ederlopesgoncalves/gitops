apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goapp
spec:
  rules:
  - host: eder-app.54.173.227.103.sslip.io
    http:
      paths:
      - backend:
          service:
            name: landing-page
            port:
              number: 8080
        path: /
        pathType: Exact
